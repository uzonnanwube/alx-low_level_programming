#include <stdio.h>
/**
 * main -program entry point
 *
 * Return: All the time 0 (Success)
 *
 */

int main ()
{
	char f;

	char y;

	f = 'a';
	y = 'A';
	while
		(f <= 'z') {
			putchar(f);
			f++;
		}
	while
		(y <= 'Z') {
			putchar (y);
			y++;
		}
	putchar('\n');
	return(0);
}
